<div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title">{{ post.title }}</h5>
      <p class="card-text">{{ post.content|u.truncate(80, '...') }}</p>
      <a href="#" class="btn btn-primary">Lire la suite</a>
    </div>
  </div>